function fruits(){
    let selectedFruits=["Avocado","Grapes", "Mangoes", "Orange", "kiwi"];
    for (let favourite = 0; favourite < selectedFruits.length; favourite++) {
        const element = selectedFruits[favourite];
        console.log(selectedFruits[favourite]);
        
    }
}
fruits();